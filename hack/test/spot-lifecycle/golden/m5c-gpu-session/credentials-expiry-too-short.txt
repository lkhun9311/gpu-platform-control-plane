aws sts get-caller-identity --query Account --output text
aws configure export-credentials --format process
aws sts get-caller-identity --query Arn --output text
aws sts get-caller-identity --query Account --output text
--- exit 1
--- messages
== study  M5-c sharing matrix -- does giving each tenant its own engine on a shared card protect the tail
== arms   [R1 shared timeSlicing mps], 1 repetition(s) each
== output <OUT>
== load   rate 9.85/s, 420000ms, weights premium=1 noisy=0.054 probe=0
==        (carried from the whole-card run; the pilot's job is to re-derive them for a half-card engine)
== the CLI could not export the active credential's expiry; falling back to scanning the credential cache
== credentials expire in 19 min; this session needs about 78 plus 30 of headroom
FAIL: credentials expire in 19 minutes. This session estimates 78 and requires 30 minutes of headroom on top, because the estimate is a model and the evidence download and the terminate call both come after the last cell. Re-authenticate first -- a run whose credentials die mid-flight cannot terminate its own instance, and the trap that tries will be refused. Type:  aws sso logout; aws sso login --profile <yours>
--- files
